#include "r6502.h"
#include "opcode.h"
#include "globalreg6502.h"

void ins0x90()
{
 //   bool CheckV = CV.getV();
 
   //             cFlag = status.getQ(CARRY)
    //   		if (cFlag == false);
		{
	//		status.getQ(CARRY);
		}
//		else 
		{		
	//		status.setQ(false, CARRY);
		}		
	
}
      
